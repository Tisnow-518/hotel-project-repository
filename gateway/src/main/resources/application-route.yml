spring:
  cloud:
    gateway:
      routes:
        - id: auth-route
          uri: lb://hotel-service-auth
          predicates:
            - Path=/api/auth/**
        - id: branch-route
          uri: lb://hotel-service-branch
          predicates:
            - Path=/api/branch/**
        - id: room-route
          uri: lb://hotel-service-room
          predicates:
            - Path=/api/room/**